/// This package defines worlds, the world generator and related objects.
///
/// The world gen is expected to run in a separate thread and queue worlds for creation.
module warp.worlds;

public {

    import warp.worlds.world;
    import warp.worlds.worldgen;

}
